# TEST — starter/ (the templates the installer lays down)

**PASS WHEN:** the structure-first templates ship here so a fresh install has them.

## Check
```bash
test -s CONSTITUTION.md && test -s jvc-method-card.md && test -s TEST.template.md
```
